// Generated by gpu-dialect from Rust syntax. Slang is the compiler backend.

[[vk::binding(0, 0)]] StructuredBuffer<float> input;
[[vk::binding(1, 0)]] RWStructuredBuffer<float> out;

[shader("compute")]
[numthreads(128, 1, 1)]
void gpu_polynomial_evaluate(uint3 id : SV_DispatchThreadID)
{
    uint __gpu_len_input;
    uint __gpu_stride_input;
    input.GetDimensions(__gpu_len_input, __gpu_stride_input);
    uint __gpu_len_out;
    uint __gpu_stride_out;
    out.GetDimensions(__gpu_len_out, __gpu_stride_out);
    var i = id.x;
    if ((i < __gpu_len_out))
    {
        var value = input[i];
        var squared = (value * value);
        var cubed = (squared * value);
        var curved = (cubed - ((2.0 * squared)));
        var linear = (0.5 * value);
        out[i] = (((curved + linear)) + 1.0);
    }
}
